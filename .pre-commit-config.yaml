repos:
  -   repo: local
      hooks:
        -   id: custom-clang-format
            name: Custom Clang-Format
            entry: ./.cl-format-exe
            language: system
            types: [file]
